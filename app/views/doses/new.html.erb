<%= simple_form_for [@cocktail, @dose] do |f| %>
  <div>
  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.submit "Add the new dose", class: "btn btn-primary" %>
  </div>
<% end %>
